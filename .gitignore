# Local state — never committed
*.bak
*.log

# Email config holds an API key — never committed even if dropped into the repo by mistake
email.json
config/email.json
**/email.json

# OS clutter
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*~

# Python (if anyone adds .py helpers)
__pycache__/
*.pyc

# Node (if anyone adds JS helpers)
node_modules/
